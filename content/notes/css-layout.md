# 现代 CSS 布局备忘

## 什么时候用 Flex

- 一维排列：导航、工具条、按钮组
- 需要对齐方式有细粒度控制

## 什么时候用 Grid

- 二维布局：页面骨架、卡片墙、仪表盘
- 需要明确行列轨道

## 容器查询

当组件需要“根据自身宽度”而不是视口宽度变化时，优先考虑容器查询。

## 常见坑

- `min-height: 100%` 依赖父级高度链
- `gap` 与外边距叠加导致间距过大
- 忘记给可收缩子项设置 `min-width: 0`
